import { notFound } from "next/navigation";import { DirectoryForm } from "@/components/directory-form";import { PageHeader } from "@/components/page-header";import { RelatedShipments } from "@/components/related-shipments";import { getClientDetail } from "@/lib/data/directories";
export default async function ClientDetailPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const{item,shipments,isDemo}=await getClientDetail(id);if(!item)notFound();return <><PageHeader title={item.companyName} description={`${item.contactPerson} · ${item.email}`}/><div className="grid gap-6 xl:grid-cols-[1fr_1fr]">{isDemo?<div className="rounded-xl border border-amber-200 bg-amber-50 p-5 text-sm text-amber-800">This customer is part of the read-only portfolio dataset.</div>:<DirectoryForm type="client" item={item}/>}<RelatedShipments shipments={shipments}/></div></>}
