import { notFound } from "next/navigation";import { DirectoryForm } from "@/components/directory-form";import { PageHeader } from "@/components/page-header";import { RelatedShipments } from "@/components/related-shipments";import { getCarrierDetail } from "@/lib/data/directories";
export default async function CarrierDetailPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const{item,shipments,isDemo}=await getCarrierDetail(id);if(!item)notFound();return <><PageHeader title={item.companyName} description={`${item.country} · ${item.vehicleType} · ${item.rating}/5 rating`}/><div className="grid gap-6 xl:grid-cols-[1fr_1fr]">{isDemo?<div className="rounded-xl border border-amber-200 bg-amber-50 p-5 text-sm text-amber-800">This carrier is part of the read-only portfolio dataset.</div>:<DirectoryForm type="carrier" item={item}/>}<RelatedShipments shipments={shipments}/></div></>}
