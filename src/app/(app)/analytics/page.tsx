import {CircleDollarSign,PackageCheck,Percent,TrendingUp} from "lucide-react";import {ProfitChart,StatusChart} from "@/components/analytics/breakdown-charts";import {PerformanceChart} from "@/components/analytics/performance-chart";import {KpiCard} from "@/components/dashboard/kpi-card";import {PageHeader} from "@/components/page-header";import {Card,CardContent,CardHeader} from "@/components/ui/card";import {getReportingData} from "@/lib/data/reporting";import {formatMoney} from "@/lib/utils";
export default async function AnalyticsPage(){const report=await getReportingData();return <><PageHeader title="Analytics" description={report.isDemo?"Read-only portfolio analytics from the sample dataset.":`Live profitability analytics in ${report.currency}.`}/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><KpiCard label="Revenue" value={formatMoney(report.revenue,report.currency)} detail="Converted total" icon={CircleDollarSign}/><KpiCard label="Profit" value={formatMoney(report.profit,report.currency)} detail="Converted total" icon={TrendingUp}/><KpiCard label="Average Margin" value={`${report.averageMargin}%`} detail="Weighted margin" icon={Percent} tone="blue"/><KpiCard label="Shipments" value={String(report.shipmentCount)} detail={`${report.active} active`} icon={PackageCheck} tone="amber"/></div><div className="mt-6 grid gap-6 xl:grid-cols-2"><Card className="xl:col-span-2"><CardHeader><div><h2 className="font-semibold">Revenue, costs &amp; profit</h2><p className="text-xs text-slate-500">Last six months in {report.currency}</p></div></CardHeader><CardContent><PerformanceChart data={report.monthly} currency={report.currency}/></CardContent></Card><Card><CardHeader><h2 className="font-semibold">Shipments by status</h2></CardHeader><CardContent><StatusChart data={report.statuses}/></CardContent></Card><Card><CardHeader><h2 className="font-semibold">Profit by client</h2></CardHeader><CardContent><ProfitChart data={report.profitByClient} currency={report.currency}/></CardContent></Card><Card><CardHeader><h2 className="font-semibold">Top clients by revenue</h2></CardHeader><CardContent className="space-y-4">{report.topClients.map((client,index)=><div className="flex items-center justify-between" key={client.name}><div className="flex items-center gap-3"><span className="grid size-8 place-items-center rounded-full bg-slate-100 text-xs font-bold">{index+1}</span><span className="text-sm font-medium">{client.name}</span></div><span className="text-sm font-semibold">{formatMoney(client.revenue,report.currency)}</span></div>)}{!report.topClients.length&&<p className="py-6 text-center text-sm text-slate-500">No client revenue yet.</p>}</CardContent></Card><Card><CardHeader><h2 className="font-semibold">Top carriers</h2></CardHeader><CardContent className="space-y-4">{report.topCarriers.map((carrier,index)=><div className="flex items-center justify-between" key={carrier.name}><div className="flex items-center gap-3"><span className="grid size-8 place-items-center rounded-full bg-slate-100 text-xs font-bold">{index+1}</span><span className="text-sm font-medium">{carrier.name}</span></div><span className="text-sm text-slate-500">{carrier.completed} completed</span></div>)}{!report.topCarriers.length&&<p className="py-6 text-center text-sm text-slate-500">No completed shipments yet.</p>}</CardContent></Card></div></>}
